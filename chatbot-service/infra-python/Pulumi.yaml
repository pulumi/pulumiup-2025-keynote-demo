name: chatbot-service-python
description: A python chatbot service running on Fargate
runtime:
  name: python
  options:
    toolchain: poetry
config:
  pulumi:tags:
    value:
      pulumi:template: python
packages:
  container-app: ../../components/container-app@0.0.0
